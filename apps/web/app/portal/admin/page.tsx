// Admin console shell — feature spec §12: approval queues, category
// capacity view, order oversight, commission/payout runs, Feed It Forward
// ledger, Fundraising administration.

export default function AdminConsolePage() {
  return (
    <main>
      <h1>Findi Admin</h1>
      {/* TODO: GET /admin/suppliers/pending, /admin/categories/pending-requests,
          /admin/revenue, /feed-it-forward/ledger */}
    </main>
  );
}
